# Optional chemistry backend. The core harness runs without this file.
#
#   python3 -m pip install -r requirements-chem.txt
#   H2L_CHEM_BACKEND=rdkit PYTHONPATH=src python3 -m h2l.cli molopt-eval \
#       --pool tests/fixtures/molopt/pool_rdkit.json
#
# Pinned per R-003: an optional scientific dependency must be version-pinned and
# smoke-tested before any accuracy claim rests on it. Smoke test:
# `pytest tests/test_chem.py` (skips cleanly when RDKit is absent).
rdkit==2025.3.6
